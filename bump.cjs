const fs = require('fs'); const version = process.argv[2]; if(!version) { console.error('Please provide a version'); process.exit(1); } ['package.json', 'src-tauri/tauri.conf.json'].forEach(f => { let c = JSON.parse(fs.readFileSync(f, 'utf8')); c.version = version; fs.writeFileSync(f, JSON.stringify(c, null, 2)); }); let cargo = fs.readFileSync('src-tauri/Cargo.toml', 'utf8'); cargo = cargo.replace(/^version = ".*?"/m, 'version = "' + version + '"'); fs.writeFileSync('src-tauri/Cargo.toml', cargo); console.log('Bumped to ' + version);
